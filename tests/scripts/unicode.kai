# Unicode and Special Character Tests

# Basic unicode in strings
echo "Hello, 世界!"
echo "Emoji: 🎉🚀✨"
echo "Japanese: 会sh は素晴らしい"

# Unicode in variable values
set GREETING = "こんにちは"
echo ${GREETING}

# Unicode in object keys (JSON requires quoted keys)
set DATA = {"名前": "太郎", "age": 25}
echo ${DATA}

# Mixed scripts
set MIX = "English 日本語 Emoji🎯"
echo ${MIX}

# Special characters that need escaping
echo "tab:\there"
echo "newline:\nhere"
echo "quote:\"here\""
echo "backslash:\\here"

# Long unicode string
set LONG_JP = "これは長い日本語の文字列です。テストのために使います。"
echo ${LONG_JP}
